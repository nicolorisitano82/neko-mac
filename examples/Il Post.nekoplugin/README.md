# An example plugin

Two feeds, one of which is deliberately named after a source the app already has,
so that the rule can be seen: a plugin never shadows a built-in word.

Add it from Neko's menu: Plugins… → Add…
